# E-Lock Tracking System Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# Original E-Lock API Configuration (if still needed)
ELOCK_API_BASE_URL=https://your-original-elock-api.com/api
ELOCK_USERNAME=your_username
ELOCK_PASSWORD=your_password

# iCloud Assets Controls API Configuration
ICLOUD_API_BASE_URL=http://icloud.assetscontrols.com:8092/OpenApi

# Third-party API for assignments (existing)
LOCAL_API_BASE_URL=http://43.205.59.159:9000/api

# Database Configuration (if using database)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=elock_tracking
DB_USER=root
DB_PASSWORD=

# Authentication & Security
JWT_SECRET=your_jwt_secret_key
SESSION_SECRET=your_session_secret

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
